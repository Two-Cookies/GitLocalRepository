/**
 * 10.  正则表达式匹配
 *
 */
public class LC0006 {
   /* public boolean isMatch(String s, String p) {

        int index= 0;
        for (int i = 0;i<s.length();i++){
            if (s.charAt(i) != p.charAt(index) && p.charAt(index)!='.') return false;
            else
        }
    }*/
}
